eureka:
  client:
    serviceUrl:
      defaultZone:  http://localhost:8761/eureka/
spring:
  cloud:
    config:
      profile:  dev
      discovery:
        enabled:  true
        serviceId:  index-config-server